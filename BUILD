========= EN =========

Music UI uses some visual controls I separated as standalone components. To build UI you must install all these components first.

Music UI uses some service utils incapsulated into class Library. You must add path .\Lib to project search path

Music UI and Main menu UI use shared file CowonCommon.as

========= RU =========

Music UI использует некоторые визуальные элементы, которые я выделил в качестве самостоятельных компонентов. Чтобы собрать апплет, надо сперва установить эти компоненты.

Music UI использует некоторые служебные процедуры, заключенные в класс Library. Надо добавить .\Lib в список путей поиска проекта

Music UI и Main menu UI используют общий файл CowonCommon.as
